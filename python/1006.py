A = float(input())
B = float(input())
C = float(input())

weightA = 2.0
weightB = 3.0
weightC = 5.0
totalWeight = weightA + weightB + weightC

avg = (A * weightA + B * weightB + C * weightC)/totalWeight

print("MEDIA = %.1f" % avg)
